# POTENTIAL TODOS

* an info option that scans the folder and displays installed extensions along with their descriptions?
* installation recipe that creates .ifp folder in the home directory. Clone settings to this
* name change to "pif"?
* publish (usage: stand inside directory of etension, run "pif publish .") Answer the questions (all required) that generates the json entry, on success create a PR to the main repository, if email is supplied that will be used to notfiy approval
* "branch" under version, defaults to "main"/"master" or whatever is most common.
  